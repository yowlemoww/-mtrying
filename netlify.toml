[build]
  publish = "build/"
  command = "yarn build"

[[plugins]]
package = "netlify-plugin-cache"
[plugins.inputs]
  paths = ["build/img"]
 
  [[redirects]]
  from = "/"
  to = "/tr/"
  status = 302
  force = true
